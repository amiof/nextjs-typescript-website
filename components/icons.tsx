const auwesome: JSX.Element = < span className="material-symbols-outlined text-purple-600" >auto_awesome </span >
const moon: JSX.Element = <span className="material-symbols-outlined">clear_night</span>
const fast: JSX.Element = <span className="material-symbols-outlined">flash_on</span>
const desktop: JSX.Element = <span className="material-symbols-outlined">desktop_mac</span>
const start: JSX.Element = <span className="material-symbols-outlined">event_note</span>
const next: JSX.Element = <span className="material-symbols-outlined">next_plan</span>
export {
  auwesome,
  moon,
  fast,
  desktop,
  start,
  next,
}
